# Learning Platform API

Phase 0 scaffold: Fastify + Prisma + PostgreSQL.

## Scripts

```bash
pnpm dev          # tsx watch
pnpm db:generate  # prisma generate
pnpm db:migrate   # prisma migrate dev
pnpm db:push      # prisma db push (quick local)
```

## Health

- `GET /health`
- `GET /api/v1/health`
